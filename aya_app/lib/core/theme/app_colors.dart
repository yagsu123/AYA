import 'package:flutter/material.dart';

/// AYA colour tokens — single source of truth for all colours.
class AppColors {
  AppColors._();

  static const primary      = Color(0xFF2992D6); // AYA Blue
  static const primaryDark  = Color(0xFF1A6FAA); // Darker blue
  static const primaryLight = Color(0xFFE8F4FD); // Light blue tint
  static const amber        = Color(0xFFE8940A); // Saffron accent
  static const amberLight   = Color(0xFFFFF3DC); // Amber background
  static const amberDark    = Color(0xFF8A4F00); // Amber text
  static const surface      = Color(0xFFFFFFFF);
  static const background   = Color(0xFFF0F4F8);
  static const textPrimary  = Color(0xFF0F172A);
  static const textMuted    = Color(0xFF64748B);
  static const border       = Color(0xFFE2E8F0);
  static const success      = Color(0xFF10B981);
  static const danger       = Color(0xFFEF4444);
  static const purple       = Color(0xFF7C3AED);
  static const purpleLight  = Color(0xFFEDE9FE);
}
